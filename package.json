{
    "private": true,
    "scripts": {
        "dev": "esbuild-serve",
        "dev:vite": "vite",
        "build": "esbuild-serve --build"
    },
    "dependencies": {},
    "devDependencies": {
        "@hyrious/esbuild-serve": "^0.2.0",
        "@prefresh/vite": "^2.1.0",
        "esbuild": "^0.9.2",
        "husky": "^4.3.8",
        "preact": "^10.5.13",
        "vite": "^2.1.0"
    },
    "husky": {
        "hooks": {
            "pre-commit": "yarn build"
        }
    }
}
